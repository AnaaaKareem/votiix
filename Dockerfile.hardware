FROM node:20-alpine

WORKDIR /app

# Copy the root server.js and its package files
COPY server.js ./
COPY package*.json ./

RUN npm install --production 2>/dev/null || npm install

EXPOSE 3007

CMD ["node", "server.js"]
